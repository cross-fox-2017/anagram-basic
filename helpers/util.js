'use strict';

var models = require('../models');
var util = {};

util.anagrams = function(callback){
  // var hasil = ""
  // models.Tb_anagram.findAll().then(function(dataAnagram){
  //     dataAnagram.forEach(function(showdataAnagram){
  //        return callback(showdataAnagram)
  //     })
  // })

    // let  test = "sdfsd"
    // callback(test)
    // return ["asdf","test"]
}

module.exports = util;
